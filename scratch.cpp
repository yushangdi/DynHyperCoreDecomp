
				// std::unordered_map<Node, unsigned> deg;
				// for (auto& p: h.eList) {
				// 	deg[p.first] = p.second.size();
				// }
				// unsigned zero_cnt = 0;
				// unsigned zero_cnt2 = 0;
				// unsigned deg_sum = 0;
				// for (int i = 1; i <= scheduler.maxNodeId; ++i){
				// 	if(getApproxCoreVal(i) == 0 && deg[i] != 0){
				// 		// cout << i << endl;
				// 		deg_sum += deg[i];
				// 		zero_cnt++;
				// 	}
				// }
				// cout << zero_cnt << " " << deg_sum << endl;
				// exit(1);		